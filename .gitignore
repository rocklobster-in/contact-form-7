# ignore node dependency directories
/node_modules/

# ignore a local Composer executable
# (see the packages-update:php script in package.json)
/composer.phar

# ignore generated source maps (one per wp-scripts entry point,
# so this must stay unanchored to match at every output depth)
index.js.map
